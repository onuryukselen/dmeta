extends _layout/default

block view
  div#ui-view
    div
      div.fade-in
        div.card
          div.card-body
            div#allEventNav
            if user && (user.role == 'admin' || user.role == 'project-admin')
              div#allAdminEventNav